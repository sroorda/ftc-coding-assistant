package org.ftc.training.lesson06;

public class VirtualIntakeController {
    public IntakeOutput update(
            boolean intakePressed,
            boolean reversePressed,
            boolean objectDetected,
            boolean emergencyStop) {
        // TODO: Implement the five requirements in priority order.
        return new IntakeOutput(0.0, "Not implemented");
    }
}
